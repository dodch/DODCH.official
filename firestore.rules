rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is Admin
    function isAdmin() {
      return request.auth != null && request.auth.uid == '4JAqYb2fnEhpqaBv7xWwsFDUXun2';
    }

    // Helper: Validate String Length (prevents buffer overflows/spam)
    function isValidString(text, min, max) {
      return text is string && text.size() >= min && text.size() <= max;
    }

    // Helper: Validate Positive Number
    function isPositiveNumber(num) {
      return num is number && num >= 0;
    }

    // Products: Public Read, Admin Write
    match /products/{productId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Orders: 
    // - Create: Allowed from frontend for Spark plan (no billing account needed).
    // - Read: Admin can read all. Users can read only their own.
    // - Update: Finalized security logic
    match /orders/{orderId} {
      allow create: if request.resource.data.status == 'Pending'
                   && request.resource.data.total == (request.resource.data.subtotal + request.resource.data.shippingFee)
                   && (
                      (request.resource.data.subtotal >= 100 && request.resource.data.shippingFee == 0) ||
                      (request.resource.data.subtotal < 100 && request.resource.data.shippingFee == 7)
                   );
        
      allow read: if isAdmin() || (request.auth != null && resource.data.userId == request.auth.uid);
      
      allow update: if isAdmin()
        || (
          request.auth != null 
          && (
            // Logic: Users CANNOT change price, items, or delivery status.
            
            // Case 1: Claiming a guest order (allows connecting a guest ORD to a newly created account)
            (resource.data.userId == 'guest' 
             && request.resource.data.userId == request.auth.uid 
             && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['userId', 'hasUnseenUpdate']))
            ||
            // Case 2: Canceling their own (only allowed if status is currently Pending)
            (resource.data.userId == request.auth.uid 
             && resource.data.status == 'Pending'
             && request.resource.data.status == 'Cancelled'
             && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status']))
          )
        );
      allow delete: if isAdmin();
    }

    // Carts: Users can only read/write their own cart
    match /carts/{userId} {
      allow read, write: if request.auth != null && (request.auth.uid == userId || isAdmin());
    } 

    // Product Reviews: 
    match /product_reviews/{reviewId} {
      allow read: if true;
      allow create: if request.auth != null 
        && request.resource.data.userId == request.auth.uid
        && isValidString(request.resource.data.text, 1, 3000)
        && (request.resource.data.images is list ? request.resource.data.images.size() <= 2 : true)
        && exists(/databases/$(database)/documents/orders/$(request.resource.data.orderId))
        && get(/databases/$(database)/documents/orders/$(request.resource.data.orderId)).data.status == 'Delivered'
        && get(/databases/$(database)/documents/orders/$(request.resource.data.orderId)).data.userId == request.auth.uid;
      
      allow update: if isAdmin() 
        || (request.auth != null 
          && resource.data.userId == request.auth.uid 
          && request.resource.data.userId == request.auth.uid 
          && isValidString(request.resource.data.text, 1, 3000));
      allow delete: if isAdmin() || (request.auth != null && resource.data.userId == request.auth.uid);
    }
    
    // Public Submissions (Newsletter & Contact Form)
    match /newsletter/{document=**} {
      allow create: if true && isValidString(request.resource.data.email, 5, 100);
      allow read, update, delete: if isAdmin();
    }
    
    // Notifications: 
    match /notifications/{notifId} {
      allow create: if isAdmin();
      allow read: if isAdmin() || (request.auth != null && resource.data.userId == request.auth.uid);
      allow update: if request.auth != null 
        && resource.data.userId == request.auth.uid
        && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['isSeen']);
    }

    // User Profiles: Users can read/write their own info
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Push Notification Subscribers
    match /subscribers/{token} {
      allow read: if isAdmin();
      allow write: if true;
    }

    // --- ROCK-SOLID MESSAGES (Signature ID Deduplication) ---
    match /messages/{messageId} {
      // Rule: Allows 'create' only if the document name (Signature ID) does NOT already exist.
      allow create: if true 
        && isValidString(request.resource.data.name, 2, 100)
        && isValidString(request.resource.data.email, 5, 100)
        && isValidString(request.resource.data.message, 1, 2000)
        && request.resource.data.status == 'unread'
        && request.resource.data.createdAt is timestamp;
      
      // Rule: STRICTLY block 'update' for everyone.
      // This is what prevents duplicate Signature IDs from ever being overwritten or resent.
      allow update: if false;

      // Only the Admin can read or delete messages from the database.
      allow read, delete: if isAdmin();
    }
  }
}
